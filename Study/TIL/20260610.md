#### Spring-JDBC
```
JDBCTemplate으로 하려면 select 시 불편하다
-> BoardRowMapper.java
```

#### 메모
```
BoardServiceClient * 이 때만 Dependency Lookup -> container.getBean("boardService");

	BoardServiceImpl - 비즈니스 로직 @Service BoardDAOSpring을 @Autowired로 타입인젝션

		BoardDAOSpring - DB 연동 @Repository -> JdbcTemplate을 @Autowired로 타입인젝션
												-내가만든 클래스-
		------------------------------------------------------
												-만들어진 클래스`
			JdbcTemplate <bean>
			                
				BasicDataSource <bean> <-------> H2 Database
```

#### H2
```
Generic H2 Server
```

#### RowMapper()
```

```

#### JavaBeans
```
DB연동(DAO) + 비즈니스 로직(Client.java)
```

### MODEL1, MODEL2
![[Pasted image 20260610112213.png]]
![[Pasted image 20260610112202.png]]

#### JSP - Scriptlet
```
https://www.blog.data101.io/187
```

#### Tomcat, Jeus, WebLogic
```
모두 클래스이다
```

#### DispatcherServlet(Controller)
```
클라이언트가 요청 시 객체 생성 => Lazy-Loading
* 반대 개념 : Pre-Loading
  
1. 사용자 입력 정보 추출
2. DB 연동 처리
3. 화면 이동
```

#### 왜 Model2 아키텍처를 사용해야 하는가
```
JSP내의 자바 로직을 분리하기 위해
```

#### EL/JSTL
```
EL(Expression Language)
```

#### 스프링 MVC
![[Pasted image 20260610154238.png]]
#### XmlWebApplicationContext
```
스프링 컨테이너
```

#### Tomcat
![[Pasted image 20260610155255.png]]
Spring Container 안에 있는 게 POJO 객체임
