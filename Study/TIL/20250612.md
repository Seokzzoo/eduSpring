#### Eclipse 관련 궁금
```
facet이 뭘까
java compiler 설정은 뭘까
xml catalog는 뭘까 * .dtd파일은 뭘까
gradle, maven?
pom.xml?
.jar파일이란?
.war파일이란?
```

#### History
```
Spring, iBatis 연동 시 api를 Spring이 제공
=> SqlMapClientFactoryBean

Spring, myBatis 연동 시 api를 myBatis가 제공
=> SqlsessionFactoryBean
```

#### ibatis에서 중요한 파일
```
board-mapping(ibatis).xml -> 쿼리
sql-map-config(ibatis).xml -> 메인 설정 파일 / board-mapping(ibatis).xml를 여기에 등록해준다
```

#### sql-map-config(ibatis).xml
```
1. DataSource 설정
2. SqlMap 파일 위치 등록
```

#### 컨테이너
```
xml을 로딩해서 거기에 정의된 객체를 생성하는 구나
```

#### %lt %gt
```
lt : less than
gt : greater thatn
```

#### XML 태그 신경 안쓰고 쿼리 적는 법
```
<![CDATA[
	select *
	from board
	where seq = #seq#
]]>
```

#### Mybatis
```
https://mybatis.org/mybatis-3/ko/index.html
```
![[Pasted image 20260612143305.png]]
#### 질문
```
1. 라이브러리(jar) 파일은 어디서 설치하나요
-> 원래는 spring.io 홈페이지에서 다운로드 받았음
-> 현재는 maven
```
![[Pasted image 20260612153002.png]]
![[Pasted image 20260612153043.png]]
Group Id와 Artifact Id는 동일하게

#### pom.xml + mvnrepository.com
```
Spring dependency

<!-- Source:
https://mvnrepository.com/artifact/org.springframework/spring-webmvc -->
<dependency>
	<groupId>org.springframework</groupId>
	<artifactId>spring-webmvc</artifactId>
	<version>7.0.8</version>
	<scope>compile</scope>
</dependency>

<!-- Source: https://mvnrepository.com/artifact/org.mybatis/mybatis -->
<dependency>
	<groupId>org.mybatis</groupId>
	<artifactId>mybatis</artifactId>
	<version>3.5.19</version>
	<scope>compile</scope>
</dependency>
```

#### jar파일을 모으고 싶어
```
Project 선택 후 -> Export(.war)

.war파일은 압축된 파일이고, 압축을 해제하면 내가 프로젝트에서 사용했던 .jar(라이브러리) 파일을 확인할 수 있다
```